"use client";

import { useEffect, useMemo, useState } from "react";
import type { Session } from "@supabase/supabase-js";
import { Archive, ArrowLeft, ChevronDown, ExternalLink, ImageOff, LogIn, LogOut, Plus, Search, ShieldCheck, SlidersHorizontal, UserRound, X } from "lucide-react";
import type { NormalizedCard } from "../graders/model";
import { createBrowserRepositories } from "../collection/runtime";
import { importLocalCards, type ImportResult } from "../collection/import-local";
import type { CollectionRepository } from "../collection/repository";
import { DuplicateCardError } from "../collection/repository";
import { isSupabaseConfigured, supabase } from "../lib/supabase";

type Grader = NormalizedCard["grader"];
type Card = NormalizedCard;
const seed: Card[] = [
  {id:"demo-1",grader:"PSA",certNumber:"DEMO-PSA-001",grade:"10",year:"2023",brand:"Topps",set:"Chrome",subject:"Collection preview",cardNumber:"#1",variant:"Refractor",frontImageUrl:"",backImageUrl:"",certUrl:"",population:341,graderSpecific:{label:"Gem Mint"},addedAt:"2026-09-14"},
  {id:"demo-2",grader:"CGC",certNumber:"DEMO-CGC-002",grade:"9.5",year:"2022",brand:"Pokémon",set:"Silver Tempest",subject:"Sample slab",cardNumber:"#184",variant:"Full Art",frontImageUrl:"",backImageUrl:"",certUrl:"",population:null,graderSpecific:{},addedAt:"2026-09-13"},
  {id:"demo-3",grader:"Degree",certNumber:"DEMO-DEG-003",grade:"9",year:"2021",brand:"Panini",set:"Prizm",subject:"Demo card",cardNumber:"#7",variant:"Silver",frontImageUrl:"",backImageUrl:"",certUrl:"",population:22,graderSpecific:{centering:"95%"},addedAt:"2026-09-12"},
];
const graders: Grader[] = ["Degree","PSA","CGC","PGS","Collect Direct"];
const empty = (grader:Grader, certNumber:string):Card => ({id:crypto.randomUUID(),grader,certNumber,grade:"",year:"",brand:"",set:"",subject:"",cardNumber:"",variant:"",frontImageUrl:"",backImageUrl:"",certUrl:"",population:null,graderSpecific:{},addedAt:new Date().toISOString(),manual:true});

function CardImage({card,large=false}:{card:Card;large?:boolean}) {
  const [failed,setFailed]=useState(false);
  if (!card.frontImageUrl || failed) return <div className={`image-placeholder ${large?"large":""}`}><ImageOff/><span>Image unavailable</span></div>;
  return <img className={`slab-image ${large?"large":""}`} src={card.frontImageUrl} alt={`${card.subject} graded card`} onError={()=>setFailed(true)}/>;
}

export default function Home(){
  const [cards,setCards]=useState<Card[]>(seed),[repository,setRepository]=useState<CollectionRepository|null>(null),[session,setSession]=useState<Session|null>(null),[loading,setLoading]=useState(true),[query,setQuery]=useState(""),[grader,setGrader]=useState("All graders"),[grade,setGrade]=useState("All grades"),[sort,setSort]=useState("Newest"),[addOpen,setAddOpen]=useState(false),[detail,setDetail]=useState<Card|null>(null),[step,setStep]=useState<"lookup"|"failed"|"manual"|"preview">("lookup"),[lookupGrader,setLookupGrader]=useState<Grader>("Degree"),[cert,setCert]=useState(""),[draft,setDraft]=useState<Card|null>(null),[lookupMessage,setLookupMessage]=useState(""),[looking,setLooking]=useState(false),[notice,setNotice]=useState(""),[authOpen,setAuthOpen]=useState(false),[authMode,setAuthMode]=useState<"signin"|"signup">("signin"),[email,setEmail]=useState(""),[password,setPassword]=useState(""),[authBusy,setAuthBusy]=useState(false),[authMessage,setAuthMessage]=useState(""),[localImport,setLocalImport]=useState<{cards:Card[];count:number}|null>(null),[importResult,setImportResult]=useState<ImportResult|null>(null),[importBusy,setImportBusy]=useState(false);
  const filtered=useMemo(()=>cards.filter(c=>{const hay=[c.subject,c.set,c.brand,c.certNumber,c.cardNumber,c.grader].join(" ").toLowerCase();return hay.includes(query.toLowerCase())&&(grader==="All graders"||c.grader===grader)&&(grade==="All grades"||c.grade===grade)}).sort((a,b)=>sort==="Oldest"?a.addedAt.localeCompare(b.addedAt):sort==="Grade high"?Number(b.grade)-Number(a.grade):b.addedAt.localeCompare(a.addedAt)),[cards,query,grader,grade,sort]);

  useEffect(()=>{
    const { local } = createBrowserRepositories({ fallback: seed });
    let current = true;
    const switchRepository = async (nextSession:Session|null) => {
      const { active: next } = createBrowserRepositories({fallback:seed,client:supabase,userId:nextSession?.user.id});
      setSession(nextSession); setRepository(next); setLoading(true); setNotice(""); setImportResult(null);
      if (nextSession) setCards([]);
      try {
        const nextCards = await next.getCards();
        if (!current) return;
        setCards(nextCards);
        if (nextSession && local.hasStoredCollection()) {
          const localCards = await local.getCards();
          setLocalImport(localCards.length ? {cards:localCards,count:localCards.length} : null);
        } else setLocalImport(null);
      } catch (error) {
        if (current) setNotice(`Collection unavailable: ${error instanceof Error ? error.message : "Please try again."}`);
      } finally { if (current) setLoading(false); }
    };
    if (!supabase) { void switchRepository(null); return ()=>{current=false}; }
    const {data:{subscription}}=supabase.auth.onAuthStateChange((_event,nextSession)=>{void switchRepository(nextSession)});
    return ()=>{current=false;subscription.unsubscribe()};
  },[]);

  useEffect(()=>{const context=(document as Document & {modelContext?:{registerTool:(tool:unknown,options?:{signal:AbortSignal})=>void|Promise<void>}}).modelContext;if(!context?.registerTool)return;const lifecycle=new AbortController();void context.registerTool({name:"start_card_entry",title:"Start card entry",description:"Open the Add Card flow with a grader and certification number ready to verify.",inputSchema:{type:"object",properties:{grader:{type:"string",enum:graders},certNumber:{type:"string"}},required:["grader","certNumber"],additionalProperties:false},annotations:{readOnlyHint:false,untrustedContentHint:false},execute(input:unknown){const value=input as {grader?:Grader;certNumber?:string};if(!value.certNumber?.trim()||!graders.includes(value.grader as Grader))throw new Error("A supported grader and certification number are required.");setLookupGrader(value.grader as Grader);setCert(value.certNumber.trim());setStep("lookup");setAddOpen(true);return{status:"ready",grader:value.grader,certNumber:value.certNumber.trim()}}},{signal:lifecycle.signal});return()=>lifecycle.abort()},[]);

  async function beginLookup(){setNotice("");setLookupMessage("");if(!repository){setNotice("Your collection is still loading.");return}if(!cert.trim()){setNotice("Enter the certification number first.");return}try{if(await repository.hasCard(lookupGrader,cert)){const duplicate=cards.find(c=>c.grader===lookupGrader&&c.certNumber.replace(/\s/g,"")===cert.trim().replace(/\s/g,""));if(duplicate)setDetail(duplicate);setAddOpen(false);setNotice("This slab is already in your collection.");return}}catch(error){setNotice(`Collection unavailable: ${error instanceof Error?error.message:"Please try again."}`);return}setLooking(true);try{const response=await fetch(`/api/lookup/${encodeURIComponent(lookupGrader)}/${encodeURIComponent(cert.trim())}`);const result=await response.json() as {ok:boolean;card?:Card;message?:string};if(result.ok&&result.card){setDraft(result.card);setStep("preview")}else{setLookupMessage(result.message??"The certification could not be retrieved.");setStep("failed")}}catch{setLookupMessage("The lookup service could not be reached.");setStep("failed")}finally{setLooking(false)}}
  function beginManual(){setDraft(empty(lookupGrader,cert.trim()));setStep("manual")}
  function update(key:keyof Card,value:string){setDraft(d=>d?{...d,[key]:value}:d)}
  async function save(){if(!draft?.subject||!draft.grade){setNotice("Add at least a subject and grade.");return}if(!repository){setNotice("Your collection is still loading.");return}try{await repository.addCard(draft);setCards(await repository.getCards());setAddOpen(false);setStep("lookup");setCert("");setNotice("Card added to your collection.")}catch(error){setNotice(error instanceof DuplicateCardError?error.message:`Card not saved: ${error instanceof Error?error.message:"Please try again."}`)}}
  async function submitAuth(){if(!supabase){setAuthMessage("Cloud accounts are not configured yet. Your cards remain on this device.");return}setAuthBusy(true);setAuthMessage("");try{if(authMode==="signin"){const {error}=await supabase.auth.signInWithPassword({email,password});if(error)throw error;setAuthOpen(false)}else{const {data,error}=await supabase.auth.signUp({email,password,options:{emailRedirectTo:window.location.href.split(/[?#]/)[0]}});if(error)throw error;if(data.session)setAuthOpen(false);else setAuthMessage("Check your email to confirm your account, then sign in.")}}catch(error){setAuthMessage(error instanceof Error?error.message:"Authentication failed.")}finally{setAuthBusy(false)}}
  async function signOut(){if(!supabase)return;const {error}=await supabase.auth.signOut();if(error)setNotice(`Sign out failed: ${error.message}`)}
  async function runImport(){if(!repository||!localImport)return;setImportBusy(true);try{const result=await importLocalCards(localImport.cards,repository);setImportResult(result);setCards(await repository.getCards());setLocalImport(null)}catch(error){setNotice(`Import failed: ${error instanceof Error?error.message:"Please try again."}`)}finally{setImportBusy(false)}}

  return <main><header className="topbar"><div className="brand"><span className="brand-mark"><ShieldCheck/></span><div><b>SLABBER<span>JAWS</span></b><small>GRADED COLLECTION</small></div></div><div className="header-actions">{session?<><span className="account-email"><UserRound/>{session.user.email}</span><button className="account-button" onClick={signOut}><LogOut/> Sign out</button></>:<button className="account-button" onClick={()=>setAuthOpen(true)}><LogIn/> Account</button>}<button className="primary desktop-add" onClick={()=>setAddOpen(true)}><Plus/> Add card</button></div></header><section className="shell"><div className="intro"><div><p className="eyebrow">THE COLLECTION</p><h1>Your graded cards,<br/><em>all in one place.</em></h1></div><div className="count"><strong>{cards.length}</strong><span>{session?"CLOUD SLABS":"DEVICE SLABS"}</span></div></div>{notice&&<div className="notice">{notice}<button aria-label="Dismiss" onClick={()=>setNotice("")}><X/></button></div>}{localImport&&<div className="import-banner"><div><strong>Local cards found</strong><p>We found {localImport.count} graded {localImport.count===1?"card":"cards"} saved on this device. Importing will not delete the local copy.</p></div><button className="primary" disabled={importBusy} onClick={runImport}>{importBusy?"Importing…":"Import to My Account"}</button></div>}{importResult&&<div className="notice">Import complete — Imported: {importResult.imported} · Already in collection: {importResult.duplicates} · Failed: {importResult.failed}<button aria-label="Dismiss" onClick={()=>setImportResult(null)}><X/></button></div>}<div className="toolbar"><label className="search"><Search/><input aria-label="Search collection" value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search cards, sets, certs…"/></label><div className="filters"><label><SlidersHorizontal/><select value={grader} onChange={e=>setGrader(e.target.value)}><option>All graders</option>{graders.map(g=><option key={g}>{g}</option>)}</select><ChevronDown/></label><label><select value={grade} onChange={e=>setGrade(e.target.value)}><option>All grades</option>{["10","9.5","9","8.5","8","7"].map(g=><option key={g}>{g}</option>)}</select><ChevronDown/></label><label><select value={sort} onChange={e=>setSort(e.target.value)}><option>Newest</option><option>Oldest</option><option>Grade high</option></select><ChevronDown/></label></div></div><div className="result-head"><span>{loading?"LOADING…":`${filtered.length} ${filtered.length===1?"SLAB":"SLABS"}`}</span><i/></div>{!loading&&(filtered.length?<div className="card-grid">{filtered.map(card=><button className="slab-card" key={card.id} onClick={()=>setDetail(card)}><div className="card-art"><CardImage card={card}/><span className={`grader-pill ${card.grader.toLowerCase().replace(" ","-")}`}>{card.grader}</span></div><div className="card-copy"><div className="grade-block"><strong>{card.grade}</strong><span>GRADE</span></div><div className="card-name"><h2>{card.subject}</h2><p>{card.year} {card.brand} · {card.set}</p><small>{card.cardNumber} {card.variant&&`· ${card.variant}`}</small></div><code>{card.certNumber}</code></div></button>)}</div>:<div className="empty"><Archive/><h2>No slabs found</h2><p>{session?"Add a graded card or import cards saved on this device.":"Try clearing a filter or add a graded card."}</p></div>)}</section><button className="floating-add" onClick={()=>setAddOpen(true)}><Plus/> Add card</button>
  {authOpen&&<div className="modal-backdrop" onMouseDown={e=>{if(e.target===e.currentTarget)setAuthOpen(false)}}><section className="modal auth-modal"><button className="close" onClick={()=>setAuthOpen(false)}><X/></button><p className="eyebrow">SLABBERJAWS ACCOUNT</p><h2>{authMode==="signin"?"Sign in":"Create account"}</h2><p className="muted">{authMode==="signin"?"Open your cloud collection on this device.":"Save your collection across browsers and devices."}</p><div className="auth-tabs"><button className={authMode==="signin"?"active":""} onClick={()=>{setAuthMode("signin");setAuthMessage("")}}>Sign In</button><button className={authMode==="signup"?"active":""} onClick={()=>{setAuthMode("signup");setAuthMessage("")}}>Create Account</button></div><label className="field">Email<input type="email" autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)}/></label><label className="field">Password<input type="password" minLength={6} autoComplete={authMode==="signin"?"current-password":"new-password"} value={password} onChange={e=>setPassword(e.target.value)}/></label>{authMessage&&<p className="form-message">{authMessage}</p>}<button className="primary wide" disabled={authBusy||!email||!password} onClick={submitAuth}>{authBusy?"Please wait…":authMode==="signin"?"Sign In":"Create Account"}</button><button className="text-button" onClick={()=>setAuthOpen(false)}>Continue on This Device</button>{!isSupabaseConfigured&&<p className="configuration-note">Cloud accounts are unavailable until Supabase is configured.</p>}</section></div>}
  {addOpen&&<div className="modal-backdrop" onMouseDown={e=>{if(e.target===e.currentTarget)setAddOpen(false)}}><section className="modal add-modal"><button className="close" onClick={()=>setAddOpen(false)}><X/></button><p className="eyebrow">NEW SLAB</p><h2>{step==="manual"?"Enter card details":step==="preview"?"Verify card":"Add to your vault"}</h2>{step==="lookup"&&<><p className="muted">Start with the grading company and certification number.</p><label className="field">Grading company<select value={lookupGrader} onChange={e=>setLookupGrader(e.target.value as Grader)}>{graders.map(g=><option key={g}>{g}</option>)}</select></label><label className="field">Certification number<input value={cert} onChange={e=>setCert(e.target.value)} placeholder="e.g. 00409451" inputMode="numeric"/></label><button className="primary wide" disabled={looking} onClick={beginLookup}>{looking?"Looking up…":"Look up certificate"}</button></>}{step==="preview"&&draft&&<div className="lookup-preview"><div className="preview-image"><CardImage card={draft}/></div><div className="identity"><span>{draft.grader} · Grade {draft.grade}</span><code>{draft.certNumber}</code></div><h3>{draft.subject}</h3><p>{draft.year} {draft.brand} · {draft.set} {draft.cardNumber} {draft.variant}</p><button className="primary wide" onClick={save}>Add to collection</button><button className="text-button" onClick={()=>setStep("lookup")}><ArrowLeft/> Not this card</button></div>}{step==="failed"&&<div className="lookup-failed"><div className="status-icon">!</div><h3>We couldn’t automatically retrieve this certification.</h3><p>{lookupMessage||"The grader may block automated access or the lookup service may be unavailable."} Nothing has been added yet.</p><button className="primary wide" onClick={beginManual}>Enter manually</button><button className="text-button" onClick={()=>setStep("lookup")}><ArrowLeft/> Try another certificate</button></div>}{step==="manual"&&draft&&<><div className="manual-grid">{([['subject','Subject / card name'],['grade','Grade'],['year','Year'],['brand','Brand'],['set','Set'],['cardNumber','Card number'],['variant','Variant'],['frontImageUrl','Front image URL'],['backImageUrl','Back image URL'],['certUrl','Certification page URL']] as [keyof Card,string][]).map(([key,label])=><label className={`field ${key.includes('Url')?'full':''}`} key={key}>{label}<input value={String(draft[key]??"")} onChange={e=>update(key,e.target.value)}/></label>)}</div><div className="identity"><span>{draft.grader}</span><code>{draft.certNumber}</code></div><button className="primary wide" onClick={save}>Add to collection</button></>}</section></div>}
  {detail&&<div className="modal-backdrop"><section className="modal detail-modal"><button className="close" onClick={()=>setDetail(null)}><X/></button><div className="detail-image"><CardImage card={detail} large/></div><div className="detail-copy"><span className="grader-pill">{detail.grader}</span><h2>{detail.subject}</h2><p>{detail.year} {detail.brand} · {detail.set} {detail.cardNumber}</p><div className="big-grade"><strong>{detail.grade}</strong><span>GRADE</span></div><dl><div><dt>Certification</dt><dd>{detail.certNumber}</dd></div><div><dt>Variant</dt><dd>{detail.variant||"—"}</dd></div><div><dt>Population</dt><dd>{detail.population??"Not available"}</dd></div><div><dt>Entry</dt><dd>{detail.manual?"Manual":"Verified lookup"}</dd></div>{Object.entries(detail.graderSpecific).map(([k,v])=><div key={k}><dt>{k}</dt><dd>{String(v)}</dd></div>)}</dl>{detail.certUrl&&<a className="primary link" target="_blank" rel="noreferrer" href={detail.certUrl}>Open certification <ExternalLink/></a>}</div></section></div>}</main>;
}
